cleari()
class quadrato(x:Int, y:Int) {
  val pic = Picture.rectangle(60, 60)
  pic.setFillColor(blue)
  pic.setPosition(x, y)
}
quadrato(50, 50)

//non riesco a capire cosa impedisce il funzionamento del codice